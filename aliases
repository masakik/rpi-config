# alias to remount ro or rw
ro='mount -o remount,ro / ; mount -o remount,ro /boot; fs_mode=$(mount | sed -n -e "s/^\
/dev\/root on \/ .*(\(r[w|o]\).*/\1/p")'
rw='mount -o remount,rw / ; mount -o remount,rw /boot; fs_mode=$(mount | sed -n -e "s/^\
/dev\/root on \/ .*(\(r[w|o]\).*/\1/p")'

